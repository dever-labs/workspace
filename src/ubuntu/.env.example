# Development Container Configuration
# Copy this file to .env and customize for your environment

# User credentials (CHANGE THESE!)
DEV_PASSWORD=dev

# User ID mapping (optional - match your host user for proper file permissions)
# USER_UID=1000
# USER_GID=1000

# Timezone
TIMEZONE=America/New_York

# Git configuration (optional)
GIT_USER_NAME=Your Name
GIT_USER_EMAIL=your.email@example.com

# Workspace path (Windows path to your projects)
WORKSPACE_PATH=C:\Users\YourUsername\Projects

# Docker Host (usually not needed on Windows with Docker Desktop)
# DOCKER_HOST=unix:///var/run/docker.sock
